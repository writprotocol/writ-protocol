# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Test coverage
coverage/

# Environment and secrets
.env
.env.local
.env.*.local

# Signing keys — the engine's key lives outside the repo; never commit one
*.key
*.pem

# Claude Code — personal, per-machine settings (at any depth)
**/.claude/settings.local.json

# Editors
.idea/
.vscode/

# Logs
*.log

# Dryrun (local-only registry checkout)
dryrun/registry-checkout/

# macOS
.DS_Store
